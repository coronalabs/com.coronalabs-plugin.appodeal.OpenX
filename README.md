Sources for the plugin `plugin.appodeal.OpenX`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.OpenX" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
